
lang mcore

let foo : Int -> Int =
  lam x:Int. x

tlet T1 :: * -> * = T2

let m = { self.
  pub let x = 1
}

let main =
  let x = 5 in
  let f : Int -> Int = lam x:Int. x in
  if true then x else (compose x {pub let z = 5})
